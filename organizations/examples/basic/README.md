# Basic Organizations example

Minimal stack that can create a Sandbox OU. Authenticate with AWS SSO / profile — no access keys in tfvars.

```bash
cp terraform.tfvars.example terraform.tfvars
terraform init
terraform plan
```
