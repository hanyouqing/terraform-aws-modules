# Scripts

| Script | Purpose |
|--------|---------|
| `terraform-workspace.sh` | Select/create Terraform workspace by environment |
| `aws-use.sh` | Export `AWS_PROFILE` / region and show caller identity |

See also `../terraform-wrapper.sh` for `-e <env> <terraform-args...>`.
